{
  "$schema": "https://mwl.dev/v0.1/provider/schema.json",
  "uri": "mwl:provider.middleware/example/observe/v1",
  "codePrefix": "Observe",
  "description": "A middleware whose declared phase carries parameters but no action.",
  "attachment": ["step"],
  "phases": {
    "onEntry": {
      "parameters": { "type": "object" }
    }
  },
  "failureCatalog": { "closed": [], "open": [] }
}
